# IdentityServer4_4x_Plus_Grpc
basic GreeterService, secured with IdentityServer4 4.x

### How To Run
just build solution with `dotnet build` and run everything in order 
1) Identity Server project
2) GrpcApi project
3) GrpcClient project
